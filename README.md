
# Template 2 – Aplicación React (Vite + Nginx)

Este repositorio forma parte de la plataforma de hosting basada en contenedores del curso **Estructura del Computador II**.  
Está configurado como **Template Repository** para que cada usuario genere su propio proyecto a partir de él.

---

## 🌐 ¿Qué contiene este template?

Este template ofrece una aplicación **React** creada con **Vite**, ideal para:

- SPAs (Single Page Applications)
- Dashboards
- Frontend con componentes
- Sitios interactivos modernos

Incluye:
- Código React básico (`src/`)
- `index.html`
- `vite.config.js`
- `Dockerfile` listo para producción con Nginx

El build final se sirve en el puerto **80** interno del contenedor.

---

## 🚀 Cómo usar este repositorio como Template

1. En la parte superior, haz clic en **“Use this template”**.
2. Selecciona **“Create a new repository”**.
3. Crea un repositorio en tu cuenta personal.
4. Clónalo:
   ```bash
   git clone https://github.com/<tuUsuario>/<tuNuevoRepo>.git
   cd <tuNuevoRepo>
5. Instala dependencias:

    ```bash
    npm install
6. Ejecuta localmente si deseas:

    ```bash
    npm run dev
7. Personaliza tus componentes dentro de src/.

8. Guarda y sube tus cambios:

    ```bash
    git add .
    git commit -m "Personalizo el template React"
    git push

8. Registra tu repositorio en la plataforma de hosting.
